# Mechanical fixes — /review 2026-06-22 (build/initial-build), iteration 1

| File | Issue | Severity | Source | Fix | Status |
|---|---|---|---|---|---|
| `api/tests/ContractManager.Api.Tests/TestSeed.cs` | `EnsureUserAsync` stored the same email for every test in a class (xUnit `IClassFixture` shares the LocalDB), violating the `Users.Email` filtered-unique index and failing 7 of 24 tests | High | tests | Rewrote to embed the user OID into the local-part of the stored email (`proc+{oid}@test.local`) so per-test OIDs produce unique emails. | Applied |
| `api/tests/ContractManager.Api.Tests/ReportsTests.cs` | Active-count assertions used absolute values, but `IClassFixture`'s shared DB carries seed contracts from prior tests, breaking the absolute count | High | tests | Refactored both `ActiveRightNow_…` and `ActiveByCategory_…` tests to capture a baseline KPI snapshot and assert the *delta* introduced by this test's seeding. | Applied |
| `web/src/**/*.{ts,tsx,css,json}` (62 files) | Pre-existing prettier formatting drift across the `web/src` tree; `npm run format:check` failed project-wide | Medium | guardrail (mechanical) | Ran `npx prettier --write "src/**/*.{ts,tsx,css,json}"`. Output is the deterministic canonical formatting per `.prettierrc`. | Applied |

**Re-verification after fixes:**
- `dotnet test` — Passed: 24, Failed: 0
- `npx vitest run` — 17 passed (5 files)
- `npm run lint` — 0 errors
- `npm run format:check` — clean
- `npx tsc -b --noEmit` — clean
- `bash .claude/hooks/check-design-conformance.sh` — PASS
