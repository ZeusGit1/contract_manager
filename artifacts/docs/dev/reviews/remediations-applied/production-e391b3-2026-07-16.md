# Remediations applied — production-e391b3-2026-07-16

| File | Issue | Severity | Source | Fix | Status |
|---|---|---|---|---|---|
| web/src/**/*.{ts,tsx,css} | Prettier formatting drift on 24 changed files | Low | Format gate | Ran `prettier --write` scoped to the diff files | Applied |
| api/src/ContractManager.Api/Services/IAttachmentBlobStore.cs:61 | `FilesystemAttachmentStore._root` had no trailing separator, so a defense-in-depth `StartsWith` check could match a sibling path prefix (`/foo/bar` vs `/foo/barbaz/`). Not exploitable — the only caller passes server-generated `{guid}/{filename}` — but defense-in-depth. | Medium | security-api.md#A03 injection / path traversal | Appended `Path.DirectorySeparatorChar` to the normalized root so the prefix check is exact. Rebuild + tests still pass. | Applied |
